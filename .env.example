# Base Immich URL. /api is added automatically if omitted.
IMMICH_URL=https://photos.example.com
IMMICH_API_KEY=replace-me

# v0.1 is hard-locked to report mode and contains no write API methods.
MODE=report

# 3600 = scan hourly. Set to 0 for one scan then exit.
SCAN_INTERVAL_SECONDS=3600
IMMICH_TIMEOUT_SECONDS=30
